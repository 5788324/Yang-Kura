# U09 Validation Note

The temporary `u09-apply-player-hooks.yml` workflow applies the exact `LyricsPanel.tsx` extraction and removes itself before the final validation commit. The persistent `branch-validation.yml` workflow remains the merge gate.
