# CURRENT ROADMAP — MVP112

## 当前版本

`0.150.0-mvp112`

## 本轮目标

根据 Codex 对 MVP108 的 GUI 审查，优先修复 UI 审计阻塞项：

1. 清理旧 localStorage 中的真实绝对路径，并禁止设置页直接渲染路径。
2. 修复下载规划页通知重复 key。
3. 页面切换后恢复主滚动区顶部。
4. 将导入器首屏改成四步日常流程。
5. 诊断页默认只加载轻量摘要，高级维护信息按需挂载。
6. 补音乐库图标按钮的无障碍名称。
7. 对诊断、导入器、下载器使用懒加载，降低首包压力。

## 不做

不改扫描、index、播放器、字幕、外部打开、copy/move executor；不接 SQLite、下载器、元数据 Provider 或 mpv。
