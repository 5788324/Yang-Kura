# U09 Merge Checklist

- [ ] temporary patch workflow removed itself
- [ ] `LyricsPanel.tsx` uses both extracted hooks
- [ ] U09 verifier passes
- [ ] all existing `verify-u*.mjs` scripts pass
- [ ] `npm run verify:stable` passes on Windows
- [ ] `npm run build` passes on Windows
- [ ] no playback, Electron, importer, index or downloader behavior changed
