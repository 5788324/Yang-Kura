# MVP115 音乐元数据管理

## 已完成

- 专辑标题、艺术家、年份、类型本地编辑。
- 曲目标题、艺术家、专辑名本地编辑。
- 清除单个专辑或曲目的本地覆盖。
- 导出全部本地元数据修改为 JSON。
- 从 JSON 备份恢复覆盖数据。
- 重新读取 `library-index.json` 后继续应用本地覆盖。

## 数据边界

本轮只写入 `yang_kura_metadata_overrides_v1`，不会修改 MP3/FLAC/WAV/M4A 标签，不会写入、移动或删除真实媒体文件，不联网，不接 SQLite。
