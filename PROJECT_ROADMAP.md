# PROJECT_ROADMAP

## 当前暂停点 / 基线

```text
GitHub main：0.146.0-mvp108 / 2e4a4aa
当前源码包：0.158.0-mvp120
阶段：单个 RJ Provider 缓存与节流收口
```

MVP108 是已推送 main 的稳定基线。MVP109～MVP113 是 UI 日常化与审计修复整理包，待后续命令验收后合入。

## 短期：合入 UI 清理包

1. 合入 MVP109～MVP113。
2. 复验 MVP112 的 GUI 修复与 MVP113 的无障碍标签。
3. 只做命令验收和必要桌面快检。
4. PASS 后统一提交并推送。
5. 如果导入器 UI 小样本发现问题，只做最小 bugfix，不扩展导入器功能。

## 下一阶段候选

| 方向 | 建议优先级 | 说明 |
|---|---:|---|
| Metadata Override / 本地元数据编辑层 | 高 | 本地编辑标题、RJ号、社团、声优、标签、封面、备注、评分等；不联网抓取。 |
| UI 文案小修 | 中 | 只修主界面残留工程词，不再开大规模 UI 重构。 |
| 元数据 Provider preview | 中后 | 单个 RJ 查询→预览→用户确认→写入，不做全库自动抓。 |
| mpv 后端 | 中后 | HTMLAudio 保底，mpv 子进程作为正式播放增强路线。 |
| SQLite | 后置 | 等导入/播放/元数据真实问题更清楚后再迁移。 |
| 下载器 | 后置 | 先做导入器和本地元数据，下载器最后做。 |

## 不建议立刻做

```text
不立刻开下载器
不立刻开 SQLite
不立刻开 mpv
不立刻做大批量 move
不立刻做自动元数据全库抓取
```

## Codex 策略

Codex 只在必要实机验证 / Git 合并 / 打包验收时使用。非必要不安排 Codex，以节省额度。需要 Codex 时，同一轮直接给完整提示词。


## MVP112 UI 审计修复

当前整理包版本为 `0.150.0-mvp112`。已根据 Codex GUI 审计修复绝对路径显示、下载页重复 key、切页滚动、导入器首屏、诊断页默认负载和音乐库无障碍问题。GitHub main 的已知正式基线仍需以实际仓库 HEAD 为准。

## MVP113 无障碍标签热修复

当前整理包版本为 `0.151.0-mvp113`。Codex 对 MVP112 复验时确认主要修复均通过，仅剩音乐库与音声库的可访问标签仍包含 MVP76 工程文案。本轮已将其改为“音乐库歌曲列表”“音乐专辑列表”“音声作品列表”，不改布局或真实业务链路。GitHub main 当前仍为 `0.146.0-mvp108 / 2e4a4aa`，待 Codex 复验后统一提交 MVP109～MVP113。

## 当前阶段：本地元数据管理

MVP114～MVP115 已完成 ASMR/音乐本地元数据覆盖、音乐专辑与曲目编辑、单项还原、JSON 备份和恢复。下一阶段可做元数据搜索/筛选联动与 ASMR 单项还原，随后再评估单个 RJ Provider 预览。

## MVP117 单个 RJ Provider 预览

- 本地元数据第一阶段已收口。
- 支持粘贴标准化单个 RJ JSON 查询结果并预览字段差异。
- 候选信息只能手动填入编辑表单，仍需用户点击保存。
- 当前不联网、不自动覆盖、不写媒体文件标签。
## MVP118 单个 RJ 真实 Provider

已完成 DLsite 单个 RJ 查询、标准化、差异预览、10 分钟短期缓存与 5 秒请求节流。下一步先由 Codex 做 Windows 在线快检并合入，不进入全库批量抓取。

## MVP119 后续决策点

Codex 实网快检通过后，再在以下方向中选一个：封面候选预览、第二 Provider、元数据批量操作的只读规划，或暂停元数据转向播放器/其他功能。当前不做全库自动抓取。


## MVP120：DLsite 查询总截止时间热修复

单个 RJ 查询的默认 12 秒现在是整个查询的总预算。最多 6 个候选 URL 共享剩余时间，不再发生 6 × 12 秒的串行等待。
